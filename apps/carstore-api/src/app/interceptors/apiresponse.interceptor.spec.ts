import { ApiresponseInterceptor } from './apiresponse.interceptor';

describe('ApiresponseInterceptor', () => {
  it('should be defined', () => {
    expect(new ApiresponseInterceptor()).toBeDefined();
  });
});
