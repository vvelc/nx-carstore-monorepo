import { utilsDataValidationUtils } from './utils-data-validation-utils';

describe('utilsDataValidationUtils', () => {
  it('should work', () => {
    expect(utilsDataValidationUtils()).toEqual('utils-data-validation-utils');
  });
});
