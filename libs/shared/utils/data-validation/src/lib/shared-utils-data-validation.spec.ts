import { sharedUtilsDataValidation } from './shared-utils-data-validation';

describe('sharedUtilsDataValidation', () => {
  it('should work', () => {
    expect(sharedUtilsDataValidation()).toEqual('shared-utils-data-validation');
  });
});
